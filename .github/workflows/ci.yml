name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  ci:
    uses: jordann6/platform-guardrails/.github/workflows/ci.yml@v1
    permissions:
      contents: read
      security-events: write
